const GAME_KEY="autoservice_v03";
const defaultState={money:15000,reputation:0,xp:0,level:1,garageLevel:1,posts:1,tools:"Базовые",mechanics:1,order:{name:"Kia Ceed",problem:"Странный звук при разгоне",difficulty:"Средняя сложность",reward:4200}};
let state=loadState(),diagnosed=false,busy=false;
function loadState(){try{const s=localStorage.getItem(GAME_KEY);return s?{...defaultState,...JSON.parse(s)}:structuredClone(defaultState)}catch(e){return structuredClone(defaultState)}}
function saveState(){localStorage.setItem(GAME_KEY,JSON.stringify(state))}function money(n){return new Intl.NumberFormat("ru-RU").format(n)+" ₽"}function xpMax(){return 100+(state.level-1)*75}
function render(){moneyEl.textContent=money(state.money);reputation.textContent=state.reputation;xp.textContent=state.xp;xpMaxEl.textContent=xpMax();level.textContent=state.level;garageLevel.textContent=state.garageLevel;posts.textContent=state.posts;tools.textContent=state.tools;mechanics.textContent=state.mechanics;goalMoney.textContent=`${money(state.money).replace(" ₽","")} / 25 000 ₽`;goalXp.textContent=`${state.xp} / ${xpMax()}`;carName.textContent=state.order.name;problem.textContent=state.order.problem}
const moneyEl=document.getElementById("money"),reputation=document.getElementById("reputation"),xp=document.getElementById("xp"),xpMaxEl=document.getElementById("xpMax"),level=document.getElementById("level"),garageLevel=document.getElementById("garageLevel"),posts=document.getElementById("posts"),tools=document.getElementById("tools"),mechanics=document.getElementById("mechanics"),goalMoney=document.getElementById("goalMoney"),goalXp=document.getElementById("goalXp"),carName=document.getElementById("carName"),problem=document.getElementById("problem");
function toast(t){const e=document.getElementById("toast");e.textContent=t;e.classList.add("show");clearTimeout(toast.t);toast.t=setTimeout(()=>e.classList.remove("show"),2200)}function addXp(a){state.xp+=a;while(state.xp>=xpMax()){state.xp-=xpMax();state.level++;state.reputation+=2;toast(`🎉 Новый уровень: ${state.level}!`)}}
function work(done){if(busy)return;busy=true;let p=0;const bar=document.getElementById("workProgress");const timer=setInterval(()=>{p+=10;bar.style.width=p+"%";if(p>=100){clearInterval(timer);busy=false;done()}},90)}
diagnoseBtn.onclick=()=>{if(diagnosed)return;work(()=>{diagnosed=true;repairBtn.disabled=false;diagnoseBtn.disabled=true;orderHint.textContent="Найдена неисправность: пропуски воспламенения. Можно выполнять ремонт.";toast("🔍 Диагностика завершена!")})};
repairBtn.onclick=()=>{if(!diagnosed)return;work(()=>{state.money+=state.order.reward;state.reputation+=4;addXp(45);diagnosed=false;workProgress.style.width="0";repairBtn.disabled=true;diagnoseBtn.disabled=false;orderHint.textContent="Клиент доволен. Следующий заказ уже ждёт.";orderStatus.textContent="Заказ выполнен";render();saveState();toast(`💰 +${money(state.order.reward)} за ремонт`);setTimeout(newOrder,1200)})};
function newOrder(){const o=[['Kia Ceed','Странный звук при разгоне',4200],['Hyundai Solaris','Плавают обороты на холостом ходу',3500],['Lada Vesta','Скрип при торможении',2800],['Volkswagen Polo','Не заводится с первого раза',5100],['Toyota Corolla','Стук в передней подвеске',4600]][Math.floor(Math.random()*5)];state.order={name:o[0],problem:o[1],difficulty:'Средняя сложность',reward:o[2]};orderStatus.textContent='Новый клиент';orderHint.textContent='Сначала найди неисправность.';render();saveState()}
upgradeGarageBtn.onclick=()=>{const costs=[0,12000,30000,75000,180000],next=state.garageLevel+1;if(next>5)return toast('🏆 Максимальный уровень гаража');const cost=costs[next-1];if(state.money<cost)return toast(`Нужно ${money(cost)}`);state.money-=cost;state.garageLevel=next;state.posts=Math.min(5,state.posts+1);if(next>=3)state.tools='Профессиональные';if(next>=5)state.tools='Премиум';addXp(60);render();saveState();toast('🏠 Гараж улучшен!')};
function openModal(html){modalContent.innerHTML=html;modal.classList.add('open')}function closeModal(){modal.classList.remove('open')}closeModalBtn=document.getElementById('closeModal');closeModalBtn.onclick=closeModal;document.getElementById('modal').onclick=e=>{if(e.target.id==='modal')closeModal()};knowledgeBtn.onclick=openKnowledgeMenu;render();
